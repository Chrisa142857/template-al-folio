---
layout: default
---
<div class="post about-page">
  <div class="about-wrapper">
    <!-- Sticky profile sidebar: avatar, name, affiliation, contact links -->
    {% if page.profile %}
      <aside class="about-sidebar{% if page.profile.align == 'left' %} about-sidebar-left{% endif %}">
        <div class="about-sidebar-inner">
          {% if page.profile.image %}
            {% assign profile_image_path = page.profile.image | prepend: 'assets/img/' %}
            {% if page.profile.image_circular %}
              {% assign profile_image_class = 'img-fluid z-depth-1 rounded-circle' %}
            {% else %}
              {% assign profile_image_class = 'img-fluid z-depth-1 rounded' %}
            {% endif %}
            {%
              include figure.liquid loading="eager" path=profile_image_path class=profile_image_class
              sizes="(min-width: 768px) 16rem, 60vw" alt=page.profile.image cache_bust=true
            %}
          {% endif %}

          <h1 class="about-sidebar-name">
            {% if site.title == 'blank' %}
              <span class="font-weight-bold">{{ site.first_name }}</span> {{ site.middle_name }}
              {{ site.last_name }}
            {% else %}
              {{ site.title }}
            {% endif %}
          </h1>

          {% if page.subtitle %}
            <p class="desc about-sidebar-subtitle">{{ page.subtitle }}</p>
          {% endif %}

          {% if page.profile.more_info %}
            <div class="more-info">{{ page.profile.more_info }}</div>
          {% endif %}

          {% if page.social %}
            <div class="social about-sidebar-social">
              <div class="contact-icons">{% include social.liquid %}</div>
              {% if site.contact_note %}
                <div class="contact-note">{{ site.contact_note }}</div>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </aside>
    {% endif %}

    <div class="about-main">
      <article>
        {{ content }}

        <!-- News -->
        {% if page.news and site.announcements.enabled %}
          <h2>
            <a href="{{ '/news/' | relative_url }}" style="color: inherit">News</a>
          </h2>
          {% include news.liquid limit=true %}
        {% endif %}

        <!-- Latest posts -->
        {% if site.latest_posts.enabled %}
          <h2>
            <a href="{{ '/blog/' | relative_url }}" style="color: inherit">Latest posts</a>
          </h2>
          {% include latest_posts.liquid %}
        {% endif %}

        <!-- Selected papers -->
        {% if page.selected_papers %}
          <h2>
            <a href="{{ '/publications/' | relative_url }}" style="color: inherit">Selected publications</a>
          </h2>
          {% include selected_papers.liquid %}
        {% endif %}

        {% if site.newsletter.enabled and site.footer_fixed %}
          {% include scripts/newsletter.liquid center=true %}
        {% endif %}
      </article>
    </div>
  </div>
</div>
